@model Mini_Project.ViewModels.LoginVM
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    ViewData["Title"] = "Doctor Login | Silver & Scholar Health Scheduler";
}

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">

<style>
    :root {
        --primary-blue: #2a6ebb;
        --light-blue: #e3f2fd;
        --doctor-color: #388e3c; /* Green for doctor */
        --gradient-start: #f5f5f5;
        --gradient-end: #e0e0e0;
    }

    * {
        font-family: 'Poppins', sans-serif;
    }

    body {
        background: linear-gradient(135deg, var(--gradient-start) 0%, var(--gradient-end) 100%);
        min-height: 100vh;
        margin: 0;
        padding: 20px;
    }

    .login-wrapper {
        min-height: calc(100vh - 40px);
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .login-card {
        background: #ffffff;
        border-radius: 20px;
        box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
        overflow: hidden;
        width: 100%;
        max-width: 450px;
    }

    .login-header {
        padding: 30px 30px 20px;
        text-align: center;
        background: var(--doctor-color);
        color: white;
    }

    .login-header h1 {
        font-size: 1.8rem;
        font-weight: 600;
        margin-bottom: 5px;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
    }

    .login-header p {
        opacity: 0.9;
        font-size: 0.9rem;
    }

    .form-container {
        padding: 30px;
    }

    .form-group {
        margin-bottom: 1.5rem;
        position: relative;
    }

    .form-label {
        display: block;
        margin-bottom: 8px;
        font-weight: 500;
        color: #333;
        font-size: 0.9rem;
    }

    .form-control {
        width: 100%;
        padding: 14px 16px;
        border: 2px solid #e0e0e0;
        border-radius: 10px;
        font-size: 1rem;
        transition: all 0.3s ease;
        background: #f9f9f9;
    }

    .form-control:focus {
        border-color: var(--doctor-color);
        box-shadow: 0 0 0 3px rgba(56, 142, 60, 0.15);
        outline: none;
    }

    .input-icon {
        position: absolute;
        right: 16px;
        top: 42px;
        color: #999;
        font-size: 1.1rem;
    }

    .submit-btn {
        width: 100%;
        padding: 16px;
        border: none;
        border-radius: 10px;
        font-size: 1.1rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        margin-bottom: 1.5rem;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        color: white;
        background: var(--doctor-color);
    }

    .submit-btn:hover {
        opacity: 0.9;
        transform: translateY(-2px);
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
    }

    .alert-message {
        padding: 12px 20px;
        border-radius: 10px;
        margin: 0 30px 20px;
        font-size: 0.9rem;
        text-align: center;
    }

    .alert-error {
        background: #ffebee;
        color: #c62828;
        border: 1px solid #ffcdd2;
    }

    .register-section {
        text-align: center;
        padding-bottom: 20px;
    }

    .register-link {
        color: var(--primary-blue);
        text-decoration: none;
    }
</style>

<div class="login-wrapper">
    <div class="login-card">
        <div class="login-header">
            <h1><i class="bi bi-heart-pulse"></i> Doctor Login</h1>
            <p>Silver & Scholar Health Scheduler</p>
        </div>

        @if (!string.IsNullOrEmpty(ViewBag.ErrorMessage))
        {
            <div class="alert-message alert-error">
                <i class="bi bi-exclamation-triangle"></i> @ViewBag.ErrorMessage
            </div>
        }

        <div class="form-container">
            <form asp-action="DoctorLogin" asp-controller="Account" method="post">
                <div class="form-group">
                    <label class="form-label" asp-for="Email">Email Address</label>
                    <input asp-for="Email" class="form-control" placeholder="Enter your email" />
                    <i class="bi bi-person-badge input-icon"></i>
                    <span asp-validation-for="Email" class="text-danger small"></span>
                </div>

                <div class="form-group">
                    <label class="form-label" asp-for="Password">Password</label>
                    <input asp-for="Password" type="password" class="form-control" placeholder="Enter your password" />
                    <i class="bi bi-key input-icon"></i>
                    <span asp-validation-for="Password" class="text-danger small"></span>
                </div>
                
                <button type="submit" class="submit-btn">
                    <i class="bi bi-box-arrow-in-right"></i> Login
                </button>
            </form>
        </div>
        <div class="register-section">
            <p>Are you a patient? 
                <a asp-action="Login" asp-controller="Account" class="register-link">
                    <i class="bi bi-person-plus"></i> Login Here
                </a>
            </p>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
